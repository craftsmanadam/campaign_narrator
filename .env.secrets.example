# OpenAI (or OpenAI-compatible) API credentials
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-4o
# Optional: override base URL for local or proxy endpoints (e.g. Ollama, LiteLLM)
# OPENAI_BASE_URL=http://localhost:11434/v1
# Optional: per-call timeout in seconds
# OPENAI_TIMEOUT_SECONDS=30
